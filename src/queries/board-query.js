const query = {
    // getContent: "SELECT ? FROM anAlyst.analyst WHERE class = ? AND gender =?",
    getContent: "SELECT * FROM anAlyst.analyst WHERE class = ? AND gender =?",
};

export default query;
